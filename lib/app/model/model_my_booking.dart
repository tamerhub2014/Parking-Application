class ModelMyBooking {
  String img;
  String bookingId;
  String title;
  String address;
  String type;
  int remainTime;

  ModelMyBooking(this.img, this.bookingId, this.title, this.address, this.type,
      this.remainTime);
}
